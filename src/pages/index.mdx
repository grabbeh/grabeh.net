import Layout from '../components/ProjectsWrapper'
import projects from '../../data/projects.json'
import Projects from '../components/Projects'

export default Layout

<Projects projects={projects} />
